// position 相關 class 功能.

/**
 * 定位類型.
 * position-{type}.
 * type: static, relative, absolute, fixed, sticky.
 */

/**
 * 定位位置.
 * {property}-{position}.
 * property: top, start, bottom, end.
 * position: 0, 50, 100, 這是百分比.
 */

/**
 * 可以配合 transform: translate; 使用.
 * translate-middle-{type}
 * type: x, y, type 可有可無.
 * translate-middle 代表 transform: translate(-50%, -50%);
 */
