// margin padding 的 class 樣式.

/**
 * {property}{sides}-{breakpoint}-{size}, breakpoint 可有可無.
 *
 * property: m, p.
 * m = margin.
 * p = padding.
 *
 * sides: t, b, s, e, x, y.
 * t = top.
 * b = bottom.
 * s = left.
 * e = right.
 * x = left + right.
 * y = top + bottom.
 *
 * size: 0, 1, 2, 3, 4, 5, auto.
 */
