// RegExp.prototype.test(參數字串);
// 正規實例，在參數字串中是否符合，返回 true / false;

// RegExp.prototype.exec(參數字串);
// 正規實例，在參數字串中是否符合，返回陣列
// 若查找到指會返回第一次
// 返回的陣列陣列內容為找到的目標
// 返回的陣列.index 代表目標的位置
// 返回的陣列.input 代表參數字串
