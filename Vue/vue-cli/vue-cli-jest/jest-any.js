// jest expect 的斷句函式介紹.

/**
 * any(), 比對值的建構函式.
 *
 * 使用方式.
 *
 * let num = 1;
 * expect(num).toEqual(expect.any(Number)); // true.
 *
 */

/**
 * anyThing(), 比對值是否為 null or undefined.
 *
 * 使用方式.
 *
 * const str = "abc";
 * expect(str).toEqual(expect.anything()); // true.
 */
