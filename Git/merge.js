// git 指令介紹.

/**
 * git merge 分支名稱
 *
 * 介紹, 將兩個分支合併, 在 VSCode 內可以選擇被合併的分支是否要壓縮成一個 commit 紀錄, 有很多更詳細的使用方式, 待補充.
 *
 * 補充, 語意應該為, 將其他分支合併到, 我目前所在的分支.
 * 補充, 合併分支時, 有很高的可能性會產生衝突, 在分工的情況下通常會選擇保留衝突的部分.
 *
 * 合併分支衝突時.
 * Accept Current 保留衝突前原本的 code 內容.
 * Accept Incoming 保留衝突後新的 code 內容.
 * Accept Both 保留所有衝突的內容.
 */
